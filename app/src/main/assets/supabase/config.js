// BADEGNAN OS — optional Supabase configuration.
// The application remains fully usable in local mode when the Supabase SDK is unavailable/offline.
window.BADEGNAN_SUPABASE_CONFIG = window.BADEGNAN_SUPABASE_CONFIG || { url: '', anonKey: '' };
